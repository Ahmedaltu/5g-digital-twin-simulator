# Configuration

## config.json
Controls simulation parameters:
- `simulation_steps`: Number of time steps
- `users`: Number of users
- `total_bandwidth_mbps`: Network bandwidth
- `scheduler`: Scheduling algorithm
- `output_file`: Output CSV file path

## Extensibility
- Add new schedulers by implementing new classes and updating the config
